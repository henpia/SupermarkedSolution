@model IEnumerable<Supermarked.Models.Varegruppe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Betegnelse)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Betegnelse)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VaregruppeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VaregruppeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VaregruppeId })
        </td>
    </tr>
}

</table>
